import { Canvas, Meta } from "@storybook/addon-docs/blocks"
import * as SyncStatusStories from "./syncStatus.stories"

<Meta of={SyncStatusStories} />

### syncStatus

Renders a sync status icon with a tooltip. Used for displaying synchronization
state of items.

#### Value type

```
type value = {
  status: "synced" | "syncing" | "pending" | "partiallySynced" | "outdated" | "failed",
  tooltip?: string
}
```

| Property  | Type           | Description                                                 |
| --------- | -------------- | ----------------------------------------------------------- |
| `status`  | SyncStatusType | The sync status to display                                  |
| `tooltip` | string?        | Optional custom tooltip text (uses default if not provided) |

#### Status Types

| Status            | Icon               | Color  | Animated | Default Tooltip                                       |
| ----------------- | ------------------ | ------ | -------- | ----------------------------------------------------- |
| `synced`          | CheckCircle        | Green  | No       | Sync completed successfully.                          |
| `syncing`         | Spinner            | Gray   | Yes      | Sync in progress.                                     |
| `pending`         | DottedCircle       | Gray   | No       | Not yet started.                                      |
| `partiallySynced` | PartiallyCompleted | Orange | No       | All aggregated data was synced but at least 1 failed. |
| `outdated`        | Warning            | Orange | No       | Data might need to be synced again.                   |
| `failed`          | CrossedCircle      | Red    | No       | Sync failed.                                          |

<Canvas
  of={SyncStatusStories.Synced}
  source={{
    code: `
// Example: Synced status
render: (item) => ({
    type: 'syncStatus',
    value: {
        status: 'synced'
    }
})
`,
  }}
/>

#### Syncing

<Canvas
  of={SyncStatusStories.Syncing}
  source={{
    code: `
// Example: Syncing status
render: (item) => ({
    type: 'syncStatus',
    value: {
        status: 'syncing'
    }
})
`,
  }}
/>

#### Pending

<Canvas
  of={SyncStatusStories.Pending}
  source={{
    code: `
// Example: Pending status
render: (item) => ({
    type: 'syncStatus',
    value: {
        status: 'pending'
    }
})
`,
  }}
/>

#### Partially Synced

<Canvas
  of={SyncStatusStories.PartiallySynced}
  source={{
    code: `
// Example: Partially synced status
render: (item) => ({
    type: 'syncStatus',
    value: {
        status: 'partiallySynced'
    }
})
`,
  }}
/>

#### Outdated

<Canvas
  of={SyncStatusStories.Outdated}
  source={{
    code: `
// Example: Outdated status
render: (item) => ({
    type: 'syncStatus',
    value: {
        status: 'outdated'
    }
})
`,
  }}
/>

#### Failed

<Canvas
  of={SyncStatusStories.Failed}
  source={{
    code: `
// Example: Failed status
render: (item) => ({
    type: 'syncStatus',
    value: {
        status: 'failed'
    }
})
`,
  }}
/>

#### Custom Tooltip

Use the `tooltip` prop to override the default tooltip message.

<Canvas
  of={SyncStatusStories.CustomTooltip}
  source={{
    code: `
// Example with custom tooltip
render: (item) => ({
    type: 'syncStatus',
    value: {
        status: 'synced',
        tooltip: 'Last synced 2 minutes ago'
    }
})
`,
  }}
/>
