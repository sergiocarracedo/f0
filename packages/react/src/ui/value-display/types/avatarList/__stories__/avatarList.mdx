import { Canvas, Meta } from "@storybook/addon-docs/blocks"
import * as AvatarListStories from "./avatarList.stories"

<Meta of={AvatarListStories} />

### avatarList

Renders a list of avatars

#### Value type

```
type value = {
    avatarList: AvatarVariant[]
    max?: number // Max avatars to render
    type?: "person" | "team" | "company"
}
```

<Canvas
  of={AvatarListStories.AvatarListType}
  source={{
    code: `
render: (item) => ({
    type: 'avatarList'
    value: {
        avatarList: [],
        max: 3
    }
})
`,
  }}
/>
