import { Canvas, Meta } from "@storybook/addon-docs/blocks"
import * as FolderStories from "./folder.stories"

<Meta of={FolderStories} />

### folder

Renders a folder with a name

#### Value type

```
type value = { name: string }
```

<Canvas
  of={FolderStories.FolderType}
  source={{
    code: `
render: (item) => ({
    type: 'file',
    // With File
    value: new File(['file'], 'file.pdf', { type: 'application/pdf' })
    // or with FileDef
    value: {
      name: 'file.pdf',
      type: 'application/pdf',
    } 
  }
})
`,
  }}
/>
