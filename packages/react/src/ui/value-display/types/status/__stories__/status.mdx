import { Canvas, Meta } from "@storybook/addon-docs/blocks"
import * as StatusStories from "./status.stories"

<Meta of={StatusStories} />

### status

Renders a status tag

#### Value type

```
type value = {
    status: StatusVariant
    label: string
}
```

<Canvas
  of={StatusStories.StatusType}
  source={{
    code: `
render: (item) => ({
    type: 'status'
    value: {
        status: 'active',
        label: item.name
    }
})
`,
  }}
/>
