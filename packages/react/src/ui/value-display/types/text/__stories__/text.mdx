import { Canvas, Meta } from "@storybook/addon-docs/blocks"
import * as TextStories from "./text.stories"

<Meta of={TextStories} />

### text

Renders a text This type's render function can be used as a shortcut returning
just a string.

```
type value = string | number | undefined
// or
type value = {
    text: string | number | undefined
    placeholder?: string
}
```

<Canvas
  of={TextStories.TextType}
  source={{
    code: `
    render: (item) => 'John Doe'
    `,
  }}
/>

#### TextInputAsObject

<Canvas
  of={TextStories.TextInputAsObject}
  source={{
    code: `
render: (item) => ({
    type: 'text',
    value: 'John Doe'
})`,
  }}
/>

#### TextValueInputAsObject

<Canvas
  of={TextStories.TextValueInputAsObject}
  source={{
    code: `
render: (item) => ({
    type: 'text',
    value: {
      text: 'John Doe'
    }
})`,
  }}
/>

#### With placeholder

<Canvas
  of={TextStories.TextWithPlaceholder}
  source={{
    code: `
render: (item) => ({
    type: 'text'
    value: {
      text: undefined
      placeholder: 'Some placeholder'
    }
})`,
  }}
/>
